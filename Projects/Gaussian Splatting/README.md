SplaTAM/
Modified SplaTAM configuration and rasterizer files used for asymmetric tracking/mapping render-budget experiments on Replica.

Gaussian-Splatting/
Modified 3DGS files used for image downscaling and early transmittance termination experiments.

Reduction-Tree/
Modified 3DGS files used for the reduction-tree compositing baseline. The auxiliary and rasterizer files implement the prefix/reduction logic, and train.py exposes the corresponding experimental setting.